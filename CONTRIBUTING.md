# Contributing

1. Keep the server read-only.
2. Never commit credentials, account identifiers, real holdings or local cache files.
3. Add or update tests for data mapping, analysis and MCP tool behavior.
4. Run `npm run check` and `npm test` from `plugins/vesper-codex`.
5. Document user-visible changes in `plugins/vesper-codex/README.md`.
